# Story 5.0: Polish + Infrastructure + Observability

**Sprint:** 5
**Epic:** Resolucao de Debitos Tecnicos
**Priority:** Medium
**Estimated Points:** 13
**Estimated Hours:** 30-53

## Objetivo

Clean up deprecated API usage, complete the BidIQ-to-Descomplicita branding migration, add structured logging with correlation IDs, integrate Sentry for production error monitoring, and resolve remaining low-priority code quality items. After this sprint, the codebase will have zero deprecated API warnings, full brand consistency, and production-grade observability.

## Debts Addressed

| ID | Debt | Hours | Owner |
|----|------|-------|-------|
| TD-015 | `datetime.utcnow()` deprecated (Python 3.12+) | 1-2 | Backend |
| TD-016 | Branding inconsistency -- BidIQ remnants in docker-compose, README, pyproject.toml | 4-8 | Both |
| TD-017 | No request/correlation ID logging | 4-8 | Backend |
| TD-018 | Hardcoded PNCP base URL in client | 1-2 | Backend |
| TD-020 | Filter diagnostic code left in production | 1-2 | Backend |
| TD-022 | No OpenAPI schema for result endpoint | 2-4 | Backend |
| TD-024 | `asyncio.get_event_loop()` deprecated (Python 3.10+) | 1-2 | Backend |
| TD-025 | No graceful shutdown (SIGTERM handler) | 4-8 | Backend |
| TD-033 | f-string in logger calls (prevents lazy formatting) | 2-4 | Backend |
| TD-035 | Frontend emoji hardcoded in source code | 1 | Frontend |
| TD-036 | No content-length validation for downloads | 1-2 | Backend |
| TD-037 | Date range max removed (unbounded queries possible) | 1-2 | Backend |
| TD-038 | Module-level singleton in `backend/job_store.py` | 1 | Backend |
| TD-039 | Deprecated `performance.timing` API in AnalyticsProvider | 1 | Frontend |
| TD-057 | No Sentry/APM integration (errors go unobserved) | 4-8 | Both |

## Tasks

### Deprecated API Cleanup

- [ ] Task 1: TD-015 -- Replace all `datetime.utcnow()` calls with `datetime.now(timezone.utc)` in production code AND test files (`backend/tests/`). Remove the `DeprecationWarning` filter from `pyproject.toml` after migration.
- [ ] Task 2: TD-024 -- Replace all `asyncio.get_event_loop()` calls with `asyncio.get_running_loop()` in production code AND test files. Verify no test uses deprecated loop creation patterns.
- [ ] Task 3: TD-039 -- Replace `performance.timing` usage in `frontend/app/components/AnalyticsProvider.tsx` with `performance.getEntriesByType('navigation')` (PerformanceNavigationTiming API).
- [ ] Task 4: TD-038 -- Remove the module-level singleton in `backend/job_store.py` (should already be replaced by DI from Sprint 4; verify and clean up any remnants).

### Branding Completion

- [ ] Task 5: TD-016 -- Search all files for "BidIQ" references and update to "Descomplicita". Key locations: `docker-compose.yml` (service names, labels), `README.md`, `pyproject.toml` (project name, description), `backend/` module docstrings, CORS comment in `backend/main.py`.
- [ ] Task 6: TD-016 -- Verify favicon (TD-029 from Sprint 3) was updated. If not, update here.
- [ ] Task 7: TD-035 -- Remove hardcoded emoji characters from frontend source files. Replace with CSS-based icons or remove decorative emoji from component strings.

### Observability

- [ ] Task 8: TD-017 -- Add correlation ID middleware: generate UUID per request, attach to all log entries, return in `X-Request-ID` response header. Use Python `logging` with structured JSON formatter.
- [ ] Task 9: TD-017 -- Update all existing log calls to include correlation ID context.
- [ ] Task 10: TD-057 -- Install `sentry-sdk[fastapi]` in backend. Configure with `SENTRY_DSN` environment variable. Set up error capture, performance tracing (sample rate configurable).
- [ ] Task 11: TD-057 -- Install `@sentry/nextjs` in frontend. Configure with `NEXT_PUBLIC_SENTRY_DSN`. Set up error boundary integration and client-side error capture.
- [ ] Task 12: TD-033 -- Replace all f-string logger calls (e.g., `logger.info(f"message {var}")`) with lazy formatting (e.g., `logger.info("message %s", var)`) across all backend files.

### API Quality

- [ ] Task 13: TD-022 -- Add Pydantic response model for the search result endpoint. Ensure OpenAPI schema is auto-generated by FastAPI and includes all result fields with descriptions.
- [ ] Task 14: TD-018 -- Extract PNCP base URL to environment variable `PNCP_BASE_URL` with default value. Update `backend/clients/` to read from config.
- [ ] Task 15: TD-020 -- Remove or gate filter diagnostic print/log statements behind debug log level. Search for `print()` calls in production code and replace with proper logging.

### Safety

- [ ] Task 16: TD-036 -- Add content-length validation for Excel download responses. Reject or warn if file exceeds configurable maximum (e.g., 50MB).
- [ ] Task 17: TD-037 -- Re-introduce configurable maximum date range (default 90 days) via environment variable `MAX_DATE_RANGE_DAYS`. Return 422 if exceeded.
- [ ] Task 18: TD-025 -- Implement SIGTERM handler for graceful shutdown: finish in-progress jobs (or mark as interrupted), close Redis connections, flush Sentry events, then exit.

## Criterios de Aceite

- [ ] Zero `DeprecationWarning` when running `pytest -W error::DeprecationWarning` (all deprecated APIs replaced)
- [ ] `grep -ri "BidIQ" --include="*.py" --include="*.ts" --include="*.tsx" --include="*.yml" --include="*.yaml" --include="*.json" --include="*.md" .` returns zero matches (excluding CHANGELOG/history files)
- [ ] Every backend log line includes a correlation ID (UUID format)
- [ ] `X-Request-ID` header present in all API responses
- [ ] Sentry receives test error from both backend and frontend (verified in Sentry dashboard)
- [ ] PNCP base URL is configurable via environment variable
- [ ] OpenAPI docs (`/docs`) show typed response schema for search result endpoint
- [ ] No `print()` calls in production backend code
- [ ] No f-string in any `logger.*()` call
- [ ] Date range > 90 days returns 422 validation error
- [ ] SIGTERM causes graceful shutdown (verified with `docker stop` observing clean exit)
- [ ] No hardcoded emoji in frontend source files
- [ ] `performance.timing` is not referenced anywhere in frontend code

## Testes Requeridos

- [ ] Unit test: Correlation ID middleware generates UUID and attaches to response header
- [ ] Unit test: Correlation ID present in log output
- [ ] Unit test: Sentry captures exception (mock Sentry SDK, verify `capture_exception` called)
- [ ] Unit test: PNCP client uses configured base URL from environment
- [ ] Unit test: Date range validation rejects > 90 days
- [ ] Unit test: Content-length validation for downloads
- [ ] Unit test: OpenAPI schema includes result endpoint response model
- [ ] Integration test: SIGTERM triggers graceful shutdown (process exits cleanly, no orphaned jobs)
- [ ] Regression: All existing backend and frontend tests still pass
- [ ] Run `pytest -W error::DeprecationWarning` -- zero warnings

## Dependencias

- Blocked by: Story 0.0 (tests must be green). TD-038 cleanup depends on Sprint 4 DI refactor.
- Blocks: None. This is the final scheduled sprint.
- Note: TD-015 and TD-024 must include test file updates per QA review (G-09).

## Definition of Done

- [ ] Code reviewed
- [ ] All tests passing
- [ ] Zero deprecated API warnings in test output
- [ ] No "BidIQ" references in codebase (verified by grep)
- [ ] Sentry integration verified in staging environment
- [ ] Correlation IDs visible in log output
- [ ] `.env.example` updated with all new environment variables (SENTRY_DSN, PNCP_BASE_URL, MAX_DATE_RANGE_DAYS)
- [ ] Documentation updated (README, deployment guide)
- [ ] No regressions
